%
% Paprastas skriptas
%

x = 1:32;
y = x.^2;

plot(x, y, 'o-r', x, y/3, 'xb')
title('Dvi funkcijos')
xlabel('X-ai')
ylabel('F_1 [-o-]  |  F_2 [-x-]')

help sin
doc plot
docsearch title

x1=1;
x2=5;
n=50;
linspace(x1,x2,n);
A=y;
size(A)
max(A)

N=1;
v = (N + 1) : 0.5 : (N + 4);
A = [1,2,3;2,2,2;3,4,5]
el_a = A(3, 2)
el_b = A(2:3, 1:2)
el_c = A([1, 3], [1, 3])
v_mod = v(1:3); 
A_nauja = [A v_mod']

